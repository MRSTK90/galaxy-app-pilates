spring:
  profiles:
    active: test
  output:
    ansi:
      enabled: always
  jpa:
    properties:
      hibernate:
        highlight_sql: true
        show_sql: true
        format_sql: true


logging:
  level:
    org:
      springframework: debug
      hibernate:
        orm:
          jdbc:
            bind: trace
